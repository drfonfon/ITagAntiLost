<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View"/>

        <variable
            name="device"
            type="com.fonfon.noloss.lib.Device" />

        <variable
            name="alarm"
            type="boolean"/>
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="@dimen/spacing_small">

        <android.support.v7.widget.AppCompatImageView
            android:id="@+id/device_image"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:layout_marginEnd="@dimen/spacing_small"
            android:src="@mipmap/ic_launcher"
            app:stringImage="@{device.image}"/>

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_gravity="center_vertical">

            <android.support.v7.widget.AppCompatTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_marginBottom="@dimen/spacing_small"
                android:text="@{device.name}"
                android:textColor="@color/oil"
                android:textSize="@dimen/text_size_large"
                android:textStyle="bold"
                android:fontFamily="monospace"
                tools:text="name" />

            <include
                android:id="@+id/connect"
                layout="@layout/layout_connect"/>

        </LinearLayout>

        <android.support.v4.widget.Space
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"/>

        <android.support.design.widget.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="@{device.isConnected ? View.VISIBLE : View.GONE}"
            android:src="@{alarm ? @drawable/ic_volume_off : @drawable/ic_volume_up}" />

    </LinearLayout>

</layout>